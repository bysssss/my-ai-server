services:
  app:
    image: my-ai-server:1.0.0-local
    environment:
      - MY_STAGE=local
      - MY_VERSION=0.1.0
      - MY_NAME=my-ai-server
      - MY_LOG=DEBUG
    volumes:
      - ./src/app:/src/app
    ports:
      - 5555:8080
