# my-ai-server — AI 개발 가이드

> AI 에이전트가 작업할 때 따르는 **공통 규칙**이다. 어떤 AI든 이 문서를 따른다 (`CLAUDE.md` 는 이 파일을 가리키는 심볼릭 링크).
> 스택별 규칙은 [`backend/AGENTS.md`](backend/AGENTS.md) · [`frontend/AGENTS.md`](frontend/AGENTS.md) 를 따른다.
>
> 장 번호는 인용을 위한 것이다. 다른 문서·스킬은 규칙을 "N장"으로 가리킨다.

---

## 1. 프로젝트 개요

AI 에이전트 개발 패턴을 정리·시연하는 레퍼런스 겸 새 작업용 베이스 템플릿. BE/FE를 한 레포에 두는 **모노레포**.

**네이밍 원칙**: `my-ai-server` = 프로젝트/서버/인프라 전반(몸통), `Abyssey` = 그 위의 데모 서비스(작업물). 서버·배포 단위는 프로젝트명, 사용자에게 보이는 페이지·도메인은 서비스명. (예외: GCP 프로젝트 ID `abyssey`는 초기 명명 — ID 불변이라 유지)

```
backend/    # FastAPI (Python). 스택 규칙: backend/AGENTS.md
frontend/   # React + TS + Vite (Abyssey 랜딩). 스택 규칙: frontend/AGENTS.md
docs/       # 개념·패턴 문서
specs/      # 작업 단위 스펙 문서 (SDD)
skills/     # 반복 절차 스킬 (.claude/skills 가 심볼릭 링크)
```

### 프레임워크 버전 함정 (에이전트 필독)

에이전트의 학습 데이터는 최신 버전보다 뒤처져 있다. 아래 항목은 **추측하지 말고 `pyproject.toml`·`package.json`·기존 코드를 먼저 확인한다.**

- **Pydantic v2** — `@validator` 가 아니라 `@field_validator`, `.dict()` 가 아니라 `.model_dump()`, `Config` 클래스가 아니라 `model_config`. v1 문법은 쓰지 않는다.
- **pydantic-settings** — 설정은 `pydantic` 이 아니라 `pydantic_settings` 에서 가져온다 (v1 시절엔 pydantic 본체에 있었다).
- **Tailwind v4** — 설정이 `tailwind.config.js` 가 아니라 CSS 의 `@import "tailwindcss"` + `@theme` 기반이다. v3 방식 설정 파일을 만들지 않는다.
- **React 19** — `forwardRef` 없이 `ref` 를 prop 으로 받을 수 있다. 옛 패턴을 새로 도입하지 않는다.
- 새 의존성을 추가할 때 구버전 시절 패키지명을 추측으로 쓰지 않는다. 버전을 확인한 뒤 추가한다.

---

## 2. 검증 명령

코드를 고쳤으면 **최소한 아래가 통과해야 한다.** 문서만 고쳤으면 생략한다.

```bash
# backend
cd backend && uv run ruff check .                              # 린트 (warning 도 0)
cd backend && uv run pytest -q --cov=app --cov-fail-under=100  # 테스트 + 커버리지 게이트

# frontend
cd frontend && npm run lint            # oxlint (warning 도 0)
cd frontend && npm run build           # tsc -b + vite build — 타입 검사까지 함께 돈다

# 로컬 실행 (도커)
make be-up / be-down / be-logs / be-ps
```

- 파일 수정마다 PostToolUse 훅이 lint 를 자동 실행한다 (7장).
- 서버 포트: 로컬 도커 `5555`. 헬스 체크: `GET /health`.

---

## 3. 사용자 권위 (최우선)

### 사용자가 "틀렸다"고 하면

1. 예외 없이 즉시 받아들인다
2. 의심·변명·해명하지 않는다 ("하지만", "원래는" 금지)
3. 즉시 고친다

### 불확실할 때

1. 즉시 멈춘다
2. 추측·가정하지 않는다
3. 확인을 요청하고 기다린다

### 범위 제한 (Scope Restriction)

- "조사만", "구현하지 마", "파일 쓰지 마" 같은 범위 제한 지시는 사용자가 **명시적으로 해제할 때까지** 유효하다.
- "이것 확인해줘" 같은 요청은 범위 제한을 해제하지 않는다.
- "멈춰", "하지 마"는 진행 중인 작업까지 전부 즉시 중단하라는 뜻이다.

---

## 4. 선(先)조사 · 후(後)구현

요청받자마자 코딩하지 않는다. 순서는 이렇다.

1. **맥락 확인** — 제공된 링크·문서·기존 코드를 먼저 본다. 모호하면 멈추고 묻는다.
2. **범위 최소화** — 가장 작은 단위로 좁힌다. 요청 범위를 벗어난 전역 수정·임의 리팩토링을 하지 않는다.
3. **스펙 작성** — 기능 추가·의미 있는 변경은 `specs/` 에 먼저 쓰고 **사용자 승인 후 구현**한다 (5장).
4. **구현**

### 코드 읽기 (Code Reading Protocol)

- 호출하는 함수/메서드가 있으면 **그 본문을 끝까지 읽기 전에 동작을 단정하지 않는다.** 일부 분기만 보고 결론 내리지 않는다.
- 조건 분기(`if`, `match`)는 **모든 분기를 확인한 뒤** 판단한다.
- 응답·토큰을 아끼려고 코드 추적을 생략하지 않는다. 광범위 탐색은 서브에이전트에 위임한다.

### Active Path Rule (구현 위치 확인)

어떤 함수·라우터에 기능을 넣기 전에, 그것이 **실제로 호출·실행되는 경로**인지 코드로 추적해 확인한다. 이름·문서·계획서만 보고 위치를 정하면 죽은 코드에 구현하게 된다. **코드가 진실이고, `02_plan.md` 와 다르면 계획서를 먼저 고친 뒤 구현한다.**

### 불필요한 코드 생성 금지

- 요청하지 않은 모듈·클래스·엔드포인트를 만들지 않는다. 기존 것으로 해결 가능한지 먼저 본다.
- 기존 스키마 확장으로 해결되는 문제에 새 엔드포인트를 추가하지 않는다.
- **미래 요구를 미리 반영하지 않는다.** 가장 단순한 방법으로 한 가지 문제만 해결한다. "나중에 필요할 것 같아서" 미리 만드는 것은 금지다.
- 불확실하면 구현 전에 방향을 확인한다.

---

## 5. 작업 관리 (specs/)

- 새 작업(기능·수정)은 `specs/YYYY-MM-DD-<슬러그>/` 폴더 단위로 관리한다 (날짜 prefix로 정렬·추적). **코딩 전 문서 작성은 필수다.**
- 시작 전 `specs/_template/` 를 복사해 `01_README`(목적·범위)·`02_plan`(접근·단계)을 채우고 **사용자 승인을 받는다.**
- 폴더는 번호 순서의 4파일, 각자 의미 하나: `01_README`(목적·범위) / `02_plan`(계획·단계) / `03_progress`(시간순 로그) / `04_notes`(결정·함정·왜).
- 작업이 진행되면 **요청 없이도** `03_progress.md` 와 `02_plan.md` 체크리스트를 즉시 갱신한다 (한 단위 완료 → 문서 갱신). 여러 작업을 몰아서 일괄 갱신하지 않는다.
- 세션이 끝날 땐 `03_progress.md` 의 **Reboot Check**(하던 일/다음/만진 파일/결정/미해결)를 갱신한다.
- `04_notes.md` 에는 결정·함정(gotcha)·"왜 이렇게 했나"를 누적한다. 코드엔 결과만 남고 이유는 안 남으므로, 다음 수정 때 같은 삽질을 막는 용도다.
- 코드베이스 탐색이 필요하면 `Explore`/`Plan` 서브에이전트에 위임한다 (메인 컨텍스트 절약). 경로를 이미 아는 단일 파일 확인·문서 작성은 직접 한다.
- 형식과 작성 규칙은 [`specs/README.md`](specs/README.md) 와 템플릿을 따른다. 여기에 중복 기술하지 않는다.

---

## 6. 스킬 (skills/)

- 반복 절차는 루트 `skills/<이름>/SKILL.md` 로 정의한다. `.claude/skills` 는 `skills/` 를 가리키는 심볼릭 링크 — 도구 중립 위치와 Claude Code 인식을 동시에 충족한다.
- 현재: `commit`(팀 규칙 커밋) · `create-pr`(PR 생성) · `review`(규칙·보안 리뷰).
- 위험 명령이 필요한 스킬은 SKILL.md 안에서 허용 범위(명령·대상·조건)를 명시하고 그 범위에서만 쓴다.
- SKILL.md 는 frontmatter 에 `name` · `description` · `allowed-tools` · `metadata`(version / role / applies-to)를 갖춘다.

---

## 7. 하네스 설정 (.claude/settings.json)

- 안전한 읽기·검증 명령은 권한 allowlist로 자동 허용된다 (git status/diff/log, lint/test 실행 등).
- **PostToolUse 훅이 파일 수정마다 lint를 기계적으로 강제한다** — backend `*.py` → ruff, frontend `*.ts/tsx` → oxlint `--deny-warnings`. 위반이면 수정이 차단 피드백으로 돌아온다. **warning도 0이어야 한다.**
- 산문 규칙은 모델의 준수에 의존하지만 훅은 기계적으로 막는다. 규칙을 강제로 올릴 수 있으면 훅으로 올린다.

---

## 8. 문서·주석 규칙

- **문서 제목·머리말에 날짜를 적지 않는다.** 손으로 고쳐야 해서 실제 수정과 어긋나고, **어긋난 날짜는 없는 날짜보다 나쁘다.** 최신화 시점은 git 이력이 본다.
  - 판단 기준은 **그 날짜가 낡느냐**다. "최신화 시점"을 가리키는 날짜는 낡으므로 금지고, **일어난 시점을 가리키는 날짜**는 낡지 않으므로 적어도 된다.
  - 예외(낡지 않는 날짜): `specs/` 폴더명의 날짜 prefix, 진행 로그의 날짜 항목, 조사·결정이 있었던 시점(예: "1차 조사에서 적용한 것 (2026-07-12)"), 배포본(pdf 등)의 판본 날짜.
- **설정 파일에는 "왜"를 주석으로 적는다.** 무엇을 하는지는 설정 자체가 말한다. 왜 그 값인지·바꿔도 되는지를 적는다. (`.claude/settings.json`, `Makefile`, `vite.config.ts`, CI 워크플로)
- **예시 코드에 실제 코드를 복사하지 않는다.** 문서·주석에 예시를 쓸 때는 범용적인 이름으로, 누가 봐도 예시임을 알 수 있게 최소한만 쓴다. 제품의 실제 클래스명·비즈니스 로직을 옮기면 예시가 낡고 진실과 어긋난다.
- **이 문서에 장을 넣거나 빼면, 장 번호를 인용하는 모든 파일(스킬·docs·specs 템플릿)을 전수 점검한다.** 번호가 밀리면 인용이 엉뚱한 장을 가리킨다 — 회사 레포에서 실제로 발생한 결함이다.
- 각 파일은 그 자체로 의미를 갖는다. 같은 내용을 두 문서에 중복 기술하지 않고 한쪽으로 링크한다.

---

## 9. 인프라 / 배포

- 배포: **BE = Cloud Run (GCP 프로젝트 `abyssey`)**, **FE = Cloudflare Pages**. (best-of-breed, 무료 → 종량)
- **AI가 인프라를 직접 제어한다.** GCP는 `secrets/gcp-sa.json`(서비스 계정 키, gitignore)로 gcloud 인증해 운영한다:

  ```
  gcloud auth activate-service-account --key-file=secrets/gcp-sa.json
  gcloud config set project abyssey
  ```

- GitHub 작업(이슈·PR·레포 관리)은 `secrets/github-token.txt`(PAT)로 한다:
  `GH_TOKEN=$(cat secrets/github-token.txt) gh <명령>` (git push/pull 은 머신 SSH 그대로)
- **시크릿 라우팅**: 작업 대상에 맞는 키를 `secrets/`에서 골라 쓴다. 머신 전역 로그인에 기대지 않는다.
- 결정 배경·최초 셋업·배포 방법은 `docs/infra.md`, 키·시크릿 규칙은 `secrets/README.md` 참조.
- ⚠️ `secrets/` 의 키는 절대 커밋·공유 금지.

---

## 10. Git / 커밋 / PR

### 커밋·푸시 — 승인된 스펙 범위 안에서는 자동

- **스펙(`02_plan.md`) 승인은 그 범위의 커밋·푸시·PR 생성·CI 대응·재수정까지 포함하는 승인이다.** 커밋마다 사람이 끼면 개발→테스트→리뷰→재수정 루프가 매번 끊긴다. 잘못된 커밋은 PR·CI 가 거르고 머지 전에 사람이 본다.
- 사람의 관문은 두 곳뿐이다: **스펙 승인(입구)과 머지(출구).**
- 승인된 스펙이 **없는** 작업(단건 수정·문서 손질 등)에서는 종전대로 사용자가 명시적으로 요청할 때만 커밋·푸시한다.
- `main` 직접 푸시 금지 — 반영은 항상 feature 브랜치 + PR 로 간다.

### 커밋 메시지

- 한국어 현재형, 요약 50자 이내, conventional prefix(`feat:` 등) 금지.
- 본문은 "무엇을"이 아니라 **"왜"** 를 쓴다 (diff 보면 아는 내용은 생략). 작성 전 `git diff --staged` 로 실제 변경을 확인한다.
- 트레일러 `Co-Authored-By` 의 모델명은 고정하지 않고 **작성 시점에 실제 응답 중인 모델명**을 넣는다.

### 금지 명령

- `git commit --amend` / `rebase -i` / `reset --hard` / `push --force` / `add -A`·`add .` 금지. 스테이지된 것만 커밋한다.

### 브랜치

- 영어 소문자 + 하이픈, 간결하게 (예: `health-endpoint`, `llm-chat`). `feat-`/`feature-` prefix·슬래시 지양.

### PR

- 제목에 `feat:`/`fix:` 같은 conventional prefix 금지. 변경 의도를 드러내는 간결한 한국어 제목.
- 본문은 변경 의도 중심, 존댓말. 이번 PR에 포함된 변경만 기술한다.
- 생성 전 `git diff main...HEAD --stat` 으로 실제 변경 파일을 확인한다.

### PR 루프 (기능 하나의 진행 순서)

1. 스펙 승인 후 구현
2. 커밋·푸시(자동) → PR 생성 → **관련 문서 갱신 커밋을 같은 PR에 포함**한다. 별도 문서 PR을 만들지 않는다 — 머지되는 시점에 코드와 문서가 한 번에 맞는다.
3. **CI 가 깨지면 로그를 읽고(짐작 금지) 고쳐서 다시 푸시한다** — 초록이 될 때까지. CI 가 빨간 채로 사용자에게 넘기지 않는다.
4. 리뷰 지적은 **코드로 검증한 뒤** 반영하거나, 근거를 들어 유보하고 회신한다. **지적이 맞아도 제안된 해법이 틀릴 수 있다** — 지적의 유효성과 해법의 타당성을 나눠 검증한다.
5. 새 지적이 없을 때까지 3~4 를 반복한다.
6. **CI 초록 + 미대응 지적 없음이 되면 "머지 가능"을 보고한다. 머지는 사용자가 한다** (명시 위임 시에만 에이전트가 실행). 머지 후 `main` 동기화·브랜치 정리를 하고 다음으로 넘어간다.

---

## 11. 한국어 의사소통

- 별도 지시 없으면 한국어 존댓말.
- "~할 시/경우" → "~하면/때", "~를 수행합니다" → "~합니다". 번역투·일본어투 지양.
- 기술 용어는 한국어·영어 병기 가능 (예: 웹소켓(WebSocket)).
- **조사는 앞 단어에 붙여 쓴다.** 인라인 코드(백틱) 뒤에도 같다 — `` `Long`으로 `` (O) / `` `Long` 으로 `` (X).

### 구어·은어·과장 표현 금지 (문서·대화·커밋·주석 모두)

| 나쁜 예 | 좋은 예 |
| --- | --- |
| 못 박았습니다 / 박아두었습니다 | 명시했습니다 / 기록했습니다 |
| 갈아엎었습니다 | 다시 작성했습니다 |
| 한 방에 | 한 번에 / 한 호출로 |
| 우겨넣었습니다 | 포함했습니다 / 추가했습니다 |
| 빡세게 | 엄격하게 |

한정 목록이 아니다. 같은 결의 표현은 모두 사실 기반 동사로 바꾼다.

---

## 12. 에이전트 메모리

- 에이전트 메모리를 **사용한다** (세션을 넘는 맥락·결정·선호 기록). 이 레포는 에이전틱 패턴 레퍼런스라 메모리도 시연 대상이다.
- 단, 구현 상세·아키텍처 결정은 메모리가 아니라 `specs/04_notes.md` 나 `docs/` 에 남긴다. 메모리는 컨텍스트를 소비하므로 최소로 유지한다.
- (회사 레포는 메모리를 금지한다. 여기서는 의도적으로 반대로 간다 — 근거는 `docs/standards-adoption.md`.)

---

## 13. 참고 문서

- 에이전틱 개발: `docs/agentic.md` — 개발 시간(에이전트로 개발) + 런타임(앱 적용 패턴) 두 층위 정리
- 인프라/배포: `docs/infra.md` — BE=Cloud Run / FE=Cloudflare 결정·최초 셋업·AI 제어법
- 회사 표준 적용 현황: `docs/standards-adoption.md` — 무엇을 적용했고 **무엇을 왜 안 했는지**
- 백로그: `docs/backlog.md` — 해야 할 일의 단일 목록 (15장)

---

## 14. 이 프로젝트에서 안 하는 것

금지 사항을 한곳에 모은 것이다. 상세는 각 장에 있다.

- 승인된 스펙 없이 사용자 지시도 없는 커밋·푸시, `main` 직접 푸시 (10장)
- `git add -A`/`git add .`, `--amend`, `rebase -i`, `reset --hard`, `push --force` (10장)
- CI 가 빨간 채로 사용자에게 넘기기 (10장)
- `02_plan.md` 사용자 승인 전 구현 시작 (4·5장)
- 호출된 함수를 끝까지 읽지 않고 동작 단정하기, 일부 분기만 보고 결론 내리기 (4장)
- 실제로 호출되지 않는 경로(죽은 코드)에 구현하기 (4장)
- 요청하지 않은 모듈·클래스·엔드포인트 만들기, 미래 요구 미리 반영하기 (4장)
- 범위 제한 지시를 사용자 해제 없이 스스로 푸는 것 (3장)
- 여러 작업을 몰아서 문서 일괄 갱신 (5장)
- 문서 제목·머리말에 날짜 적기 (8장)
- 문서·주석 예시에 실제 클래스명·비즈니스 로직 복사 (8장)
- lint warning 을 남긴 채 넘어가기 (2·7장)
- 구버전 문법으로 코드 쓰기 (Pydantic v1, Tailwind v3 설정 등 — 1장)
- `secrets/` 의 키 커밋·공유 (9장)
- 구어·은어·과장 표현 (11장)

---

## 15. 향후 방향

- **해야 할 일은 [`docs/backlog.md`](docs/backlog.md) 한곳에서 관리한다.** 여기와 spec 진행 로그에 할 일을 나눠 적지 않는다 — 작업을 시작할 때는 백로그 항목을 specs 폴더로 옮긴다.
- 확정된 방향: 브랜치 전략은 **GitHub Flow**(main + feature 브랜치 + PR — 2026-07-12 확정).
